{
  "name": "squidback",
  "version": "1.0.0",
  "description": "unleash your feedbacks",
  "main": "index.js",
  "scripts": {
    "build": "browserify src/main.js -o www/main.js",
    "watch": "watchify src/main.js -o www/main.js --debug --verbose ",
    "start": "http-server -a 0.0.0.0 -S www/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "feedback",
    "larsen",
    "performance",
    "instrument",
    "adaptive filter"
  ],
  "author": "Gianluca Elia",
  "license": "",
  "dependencies": {
    "array-smooth": "^1.0.0",
    "cubic-spline": "^3.0.3",
    "http-server": "^0.12.1",
    "rtcmulticonnection": "^3.7.0",
    "socket.io-client": "^2.2.0"
  },
  "devDependencies": {
    "browserify": "^16.5.0",
    "watchify": "^3.11.1"
  }
}
